<p id="notice"><%= notice %></p>

<!-- <p><div class="img-thumbnail" align="left">
	<%= image_tag current_user.profile_pic.url(:thumb) %>
</div>
	Hello <%= current_user.name %>, <br>
	<p style="text-indent: 30px"><font size="2">
		<%if @feeder_count < 2%>
			Baby Feeder
		<%elsif @feeder_count < 5 || @feeder_count >= 2%>
			Knight of Feeders
		<%elsif @feeder_count < 7 || @feeder_count >= 5%>
			Hero of Feeders
		<%else%>
			Legend of Feeders
		<%end%>,

		<%if @feeding_count < 2%>
			Baby Organizer
		<%elsif @feeding_count < 5 || @feeding_count >= 2%>
			Silver Organizer
		<%elsif @feeding_count < 7 || @feeding_count >= 5%>
			Super Organizer
		<%else%>
			Master Organizer
		<%end%>
	</font>
	</p>
</p>
 -->
<!-- <div class="img-thumbnail">
	<%= image_tag current_user.profile_pic.url(:thumb) %>
</div><br>

<p>
<%= image_tag @user.profile_pic.url(:thumb).to_s %>
</p>
<p>
<%= image_tag @user.profile_pic.url(:small).to_s %>
</p>
<p>
<%= image_tag @user.profile_pic.url(:mid).to_s %>
</p>
<p>
<%= image_tag @user.profile_pic.url(:full).to_s %>
</p> -->

<div class="container-fluid profilebody">
	<div class="row profile">
		<div class="col-sm-4">
		</div>
		<div class="col-sm-4">
		<section class="card">
		  <figure class="panel meta">
		  	<picture>
			<div class="img-thumbnail">
				<%= image_tag current_user.profile_pic.url(:thumb) %>
			</div>
		    </picture>
		    <figcaption>

		      <h1 class="name"><%=current_user.name%></h1>
		      <h3 class="title">		
		      	<%if @feeder_count < 2%>
					Baby Feeder
				<%elsif @feeder_count < 5 || @feeder_count >= 2%>
					Knight of Feeders
				<%elsif @feeder_count < 7 || @feeder_count >= 5%>
					Hero of Feeders
				<%else%>
					Legend of Feeders
				<%end%>,

				<%if @feeding_count < 2%>
					Baby Organizer
				<%elsif @feeding_count < 5 || @feeding_count >= 2%>
					Silver Organizer
				<%elsif @feeding_count < 7 || @feeding_count >= 5%>
					Super Organizer
				<%else%>
					Master Organizer
				<%end%>
				</h3>
		    </figcaption>
		  </figure>
		  
		  <div class="panel info">
		  	<div class = 'email'>
			  	<p><img src="https://cdn2.iconfinder.com/data/icons/New-Social-Media-Icon-Set-V11/512/email.png" alt="Email Icon" style="width:20px;height:20px;">
				<%=current_user.email%></p>
		  	</div>

		  	<div class = 'feeder'>
			  	<dt><%=link_to "Feeder", user_feeders_path(current_user)%></dt>
			  	<dd>1</dd>
				<p>You have joined <%=pluralize(@feeder_count, 'feeding')%>!</p>
		  	</div>

		  	<div class = 'feeding'>
		  	<dt><%=link_to "Feeding", user_feedings_path(current_user)%></dt>
		  	<dd>1</dd>
			<p>You have orgainized <%=pluralize(@feeding_count, 'feeding')%>!</p>
		  	</div>
			
<!-- 		    <dl class="skillz">
		      <dt>Feeder</dt>
		      <dd>80</dd>
		      <dt>Feeding</dt>
		      <dd>60</dd>
		    </dl> -->
		  </div>
		  
		</section>
		</div>
		<div class="col-sm-4">
		</div>
	</div>

</div>


<%= link_to 'Edit Your Profile', edit_user_registration_path %> |
<%= link_to 'Return to homepage ', root_path %>
